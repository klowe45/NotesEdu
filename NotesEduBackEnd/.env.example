# Database Configuration
PGUSER=your_postgres_user
PGPASSWORD=your_postgres_password
PGHOST=localhost
PGDATABASE=notesedu
PGPORT=5433
PORT=4000

# Email Configuration (Optional - for production email sending)
# Leave these blank to use test email service (Ethereal)
# For Gmail:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_SECURE=false
# EMAIL_FROM="NeuroNotes System <your-email@gmail.com>"

# For other providers (SendGrid, AWS SES, etc.), adjust accordingly
